# Copia este archivo a `.env` y completa los valores reales (nunca commitear `.env`).
# Espeja backend/.env; en Docker Compose la DB apunta al servicio `db`.

DATABASE_URL=postgresql://career_analyzer:career_analyzer_dev@db:5432/career_analyzer?schema=public
OPENAI_API_KEY=
OPENAI_MODEL=
JWT_SECRET=
CLIENT_ORIGIN=http://localhost:4200

POSTGRES_USER=career_analyzer
POSTGRES_PASSWORD=
POSTGRES_DB=career_analyzer

BACKEND_PORT=3000
FRONTEND_PORT=4200
DB_PORT=5432
NGINX_PORT=8080